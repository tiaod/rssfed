ignoredBuiltDependencies:
  - '@parcel/watcher'
  - '@tailwindcss/oxide'
  - esbuild
  - unrs-resolver

allowBuilds:
  # 前端用 IndexedDB 适配器，不需要 leveldown 原生构建；vue-demi 的 postinstall 由 Vite 处理
  leveldown: false
  vue-demi: false
