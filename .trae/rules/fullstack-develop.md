1. 项目使用Nuxt4，前端使用Nuxt UI组件库，后端使用Nuxt Server，drizzle ORM + PostgreSQL，Better Auth。
2. 项目使用vitest进行测试，测试驱动开发（TDD）。
3. 使用pnpm作为包管理器，遵循ESLint `1tbs` 风格，不使用拖尾逗号。
4. 提交前跑 `pnpm lint`、`pnpm typecheck`、`pnpm test:run`