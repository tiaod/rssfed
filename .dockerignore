# 构建上下文瘦身：这些内容要么在镜像内重新安装/构建，要么属于本地私有状态。
# 注意：Dockerfile 的 COPY . . 会应用本文件，缺了它会把手写的 node_modules 和 .env 一起带进镜像。

# ── 依赖与构建产物 ──────────────────────────────────────────────
**/node_modules
**/dist
**/.output
**/.nuxt
**/.nitro
**/.cache
**/.data
.pnpm-store
.pnpm-store-mcp
**/*.tsbuildinfo

# ── 本地环境变量与密钥：一律运行时注入，绝不进镜像 ──────────────
.env
.env.*
**/.env
**/.env.*

# ── 版本控制 / 编辑器 / 文档 ────────────────────────────────────
.git
.gitattributes
.gitignore
.github
.zcode
.trae
.dsh
docs
*.log

# ── Nuxt starter 模板残留（嵌套 workspace 文件会干扰 pnpm 根识别）──
packages/nuxt-client/pnpm-lock.yaml
packages/nuxt-client/pnpm-workspace.yaml
packages/nuxt-client/renovate.json
packages/nuxt-client/LICENSE
packages/nuxt-client/README.md
